;Happens because we don't have the foresight of knowing what's already been matched while matching a query with a pattern in a data base.
;A possible solution could be if we accumulate the results and use them as a data base to build a simple rule that fixes the problem.
