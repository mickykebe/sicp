;Ben's system won't work because as Cy has showed a pattern variable with its result can be present more than once at the end of evaluation.
;One possible solution is if instead of passing a vriable to the accumulation function to have a procedure the user crafts to filter the results.
